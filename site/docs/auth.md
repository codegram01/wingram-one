

# Auth 

## Init - When web init
- check token local
- if not have token local
    -> not logged in
- if have token 
    -> get auth info
        + if ok 
            + logged in 
        + else
            + not logged in